apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-morrisons-site
  namespace: kube-system
spec:
  secretName: wildcard-morrisons-site-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.morrisons.site"
    - "morrisons.site"
